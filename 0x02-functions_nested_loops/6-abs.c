#include "main.h"
/**
 * _abs - compute the absolute value of a given number
 * @n: integer to compute absolute number
 *
 * Return: absolute value of the input number.
 */
int _abs(int n)
{
if (n >= 0)
{
return (n);
}
else
{
return (-1 * n);
}
}
